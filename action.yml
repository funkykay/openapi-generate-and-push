name: ""
description: ""
inputs:
  generator:
    description: ""
    required: true
  spec:
    description: "the path"
    required: true
    default: "api.yaml"
  config:
    description: ""
    required: false
  repository:
    description: ""
    required: true
    default: ""
outputs:
  time:
    description: ""

runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.generator }}
    - ${{ inputs.spec }}
    - ${{ inputs.config }}
