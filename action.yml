name: ""
description: ""
inputs:
  api-spec:
    description: "the path"
    required: true
    default: "api.yaml"
  generator:
    description: ""
    required: true
  config-file:
    description: ""
    required: false
outputs:
  time:
    description: ""

runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.api_spec }}
    - ${{ inputs.generator }}
    - ${{ inputs.config_file }}
